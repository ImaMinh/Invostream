[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Redirect rules for React Router (Single Page Application routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
